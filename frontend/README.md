# Frontend Track 4 — Streamlit

Requiere que el backend (`../backend/`) esté corriendo en `http://127.0.0.1:8000`.

## Arrancar
```
pip install -r requirements.txt
streamlit run app.py
```

Se abre en el navegador automáticamente (normalmente `http://localhost:8501`).
